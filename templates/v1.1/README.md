# FSL v1.1 templates lane

This branch carries the concrete Four-Square Letterhead v1.1 document artifacts.

Verified artifacts:
- 1-page template: DOCX + PDF
- 2-page template: DOCX + PDF
- 3-page template: DOCX + PDF
- Django illustrative sample: DOCX + PDF

Every rendered PDF is validated for its expected page count and for the visible `Four-Square Template v1.1` marker. The Django sample also carries `Django Sample v1.1` and uses the illustrative `SPEC / CODE / DOCS / MEMORY` lane mapping.
