# FSL v1.5 — File-backed recall

FSL = Four-Square Letterhead. This artifact set uses **Four-Square Template v1.5**. It is not a claim that ChatGPT account-level Personalization Memory was updated.

## Recall the files, not only the theory

Open `README.md`, the selected 1P/2P/3P DOCX/PDF pair, `FSL-v1.5-CONSTITUTION.md`, `FSL-v1.5-SKILL.md`, the four-item Checklist and the illustrative Django sample when useful. `MANIFEST.txt` and `SHA256SUMS.txt` locate and verify the exact set. The validation report states what is proven; the changelog explains migration from v1.4.

## Fixed anchors

SPEC | <project lane A> | <project lane B> | MEMORY. Only SPEC and MEMORY are fixed. BASE and AI are examples. Skills plus plugins can share a general AI lane. Names remain consistent across versions.

Template version identifies the FSL language. Project version identifies the governed work. `main1` is the owning project's first immutable commit; initial lane branches start there; each later lane continues its prior version; `main` is accepted integration. Never confuse repositories because their branch names look alike.

## Remember the simplification

One initial line: tiny input -> required touchpoints -> observable output -> prove -> grow.

No YAML iteration schema, no full future-iteration plan and no per-iteration ledger are required. Parallel E2E, E2M, M2E and meet-in-the-middle remain available. M is a named midpoint contract. Listing a touchpoint is not evidence; partial/component checks do not establish a bypassed real path.

Essential references only: item, intent/use, canonical source. Use an exact pin only when it matters. REUSE does not automatically INHERIT; REFERENCE does not adopt law. The authority remains in project SPEC.

Footer on every page: **FSL owns the method. The project owns the work.** Consuming-project artifacts and releases stay in that project's repository; the FSL repository stays reusable and reference-only.

## Proof and status

Stable clause IDs; CLAIM / SCOPE / ENVIRONMENT / EVIDENCE / RESULT; four exact reviewed commits at release. Missing evidence remains NOT YET PROVEN. An agent does not invent missing law. MEMORY may remember law; MEMORY cannot make law.

This v1.5 package was generated locally. Repository publication status must be checked separately before making a publication claim.
