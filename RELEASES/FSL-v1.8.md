# FSL v1.8 — Plain-English Method

This release promotes the approved content-complete v1.8 candidate. Its three method sections are unchanged; only candidate/publication metadata is promoted. The original 40-item completion review remains historical evidence rather than a present-tense publication declaration.

The method is the single editable English authority adopted explicitly by SPEC. It retains concrete but flexible squares, FSL first in SPEC, R-01 routes, iterative work without compulsory iteration numbering, four recommended early E2E learning goals, optional existing-project iteration plans, scoped evidence, lifecycle and repository ownership. No touchpoint table, required YAML authoring schema or empty 2P/3P forms has been added.

Documents: three-page method, one-page project template and one-page illustrative Django example, each DOCX/PDF. Project letterheads may extend to two or three pages as needed. The build uses the approved native Word structure with source-driven method words and checks the template/sample body text against the approved candidate hashes.

The reviewed six GitHub-produced document files were downloaded, their digests rechecked, all five PDF pages visually reviewed, and all three DOCX files independently rendered and reviewed. The CI and local renderers can differ in line wrapping and PDF metadata; both retained the 3/1/1 page layout. Generated PDFs are not claimed byte-identical across renderers.

All four lane gates passed. Assembly must still check their exact combination, original v1.7 preservation, the six reviewed document bytes, clean extraction and tamper rejection. Its run record reports actual outcomes; the final artifact commit establishes branch publication.

The native-layout JSON is internal build data, not a method users must fill. No font files are distributed. Django runtime: NOT RUN. Consuming-project conformance: NOT YET PROVEN.
